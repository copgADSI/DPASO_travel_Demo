import { StyleSheet } from "react-native";

const styles_Categories = StyleSheet.create({
    categoryContainer: {
        marginTop: 60,
        marginHorizontal: 20,
        flexDirection: "row",
        justifyContent: "space-between"
        
    }
})

export default styles_Categories